"""全部参数集中在这里，长度单位为米。

每项按来源标注：
- 【几何】虚构示例路线与桥梁的设计输入，不是测量值。
- 【物理】物理常数。
- 【假设】工效、周期、设备能力、容量限制等计划输入——按预制梁场与架桥机施工的
  常见量级取值，改这里、重跑，全部产物跟着变。
"""
from datetime import date

# ------------------------------------------------------------------ 【几何】路线
START_STATION = 10000.0            # 起点桩号 K10+000
START_XY = (0.0, 0.0)
START_AZIMUTH_DEG = 30.0           # 起点方位角：自 +x 轴逆时针
R_CURVE = 700.0                    # 圆曲线半径
LS = 110.0                         # 缓和曲线长
K = 1.0 / R_CURVE
# 平曲线线元：(类型, 长度, 起点曲率, 终点曲率)。曲率为正 = 左转
H_SEGMENTS = [
    ("LINE", 320.0, 0.0, 0.0),
    ("CLOTHOID", LS, 0.0, K),
    ("CIRCULARARC", 190.0, K, K),
    ("CLOTHOID", LS, K, 0.0),
    ("LINE", 380.0, 0.0, 0.0),
]
# 竖曲线：变坡点 (桩号, 高程)，以及各变坡点的抛物线竖曲线长度（0 = 不设）
V_PVI = [(10000.0, 128.000), (10520.0, 136.320), (11110.0, 129.240)]
V_CURVE_LENGTH = [0.0, 280.0, 0.0]

# 横坡与超高：每幅绕本幅中心线旋转。slope_left 为「向左侧下降」的坡率。
CROSSFALL = 0.02                   # 直线段路拱横坡，各幅向外侧排水
SUPERELEVATION = 0.04              # 圆曲线段全超高，向曲线内侧（左）

# ------------------------------------------------------------------ 【几何】桥梁
BRIDGE_START = 10350.0             # 0 号桥台支承线桩号
N_SPANS = 12
SPAN = 30.0                        # 跨径（沿路线中线量）
DECKS = [("L", 7.0), ("R", -7.0)]  # 左幅 / 右幅中心线偏距，左正
UNITS = (4, 4, 4)                  # 分联：4×30 m + 4×30 m + 4×30 m，先简支后连续；联间设伸缩缝
DECK_WIDTH = 12.75
GIRDER_OFFSETS = (-4.9, -2.45, 0.0, 2.45, 4.9)   # 每幅 5 片 T 梁，相对幅中心（1 号梁在右）
H_GIRDER = 2.0                     # 梁高
PAVEMENT_T = 0.20                  # 10 cm 混凝土调平层 + 10 cm 沥青面层

# 梁端位置：梁端面平行于径向支承线，下面的距离都垂直于支承线量。
EXP_HALF = 0.04                    # 伸缩端：梁端距过渡墩中心线 / 桥台背墙前缘（联间缝 0.08 m）
CONT_HALF_NOM = 0.425              # 连续端：梁端距连续墩中心线的名义值（连续段宽 0.85 m）
CONT_HALF_MIN, CONT_HALF_MAX = 0.35, 0.50   # 梁长归并时允许的范围（连续段宽 0.70 – 1.00 m）
SPEC_STEP = 0.01                   # 预制梁长取整到 10 mm
BEARING_INSET = 0.35               # 伸缩端永久支座中心距梁端
TEMP_INSET = 0.35                  # 连续端临时支座中心距梁端（体系转换后拆除）

# 预制 T 梁截面（跨中），局部坐标 x 横桥向、y 竖向，梁顶中心为原点，逆时针
T_SECTION = [
    (-0.925, 0.0), (-0.925, -0.16), (-0.10, -0.25), (-0.10, -1.60), (-0.25, -1.75),
    (-0.25, -2.00), (0.25, -2.00), (0.25, -1.75), (0.10, -1.60), (0.10, -0.25),
    (0.925, -0.16), (0.925, 0.0),
]
WET_JOINT_T = 0.16                 # 湿接缝厚
WEB_HALF = 0.10                    # 腹板半厚（T_SECTION 里 x = ±0.10 的那段）
# 横隔板：每跨沿径向 5 道，梁端两道（端横隔板）+ 1/4、1/2、3/4 跨三道；在两片梁腹板之间，
# 顶面在翼缘根部（梁顶以下 0.25 m）、底面在马蹄上缘（梁顶以下 1.60 m）。修正偏心压力法的前提是
# 「桥上有可靠的横向联结」，横隔板就是这个联结，所以模型里要有它。
DIAPHRAGM_T, END_DIAPHRAGM_T = 0.18, 0.25
DIAPHRAGM_FRACTIONS = (0.25, 0.5, 0.75)
DIAPHRAGM_TOP, DIAPHRAGM_BOTTOM = -0.25, -1.60

# 下部结构。盖梁 / 台帽顶面按该处桥面横坡做成斜面、底面水平（中心处为标注高度）。
CAP_LEN, CAP_W, CAP_H = 12.2, 2.4, 1.6          # 盖梁：沿支承线长、顺桥向宽、中心高
ABUT_CAP_W, ABUT_CAP_H = 1.9, 1.2               # 桥台台帽：背墙坐在台帽后部
BACKWALL_T = 0.5                                # 桥台背墙厚（前缘即桥台支承线）
COLUMN_D = 1.5
COLUMN_OFFSETS = (-2.9, 2.9)                    # 每幅两根墩柱 / 台桩，相对幅中心
COLUMN_EMBED = 1.0                              # 墩柱底在地面以下的深度（桩柱连接处）
PILE_D, ABUT_PILE_D = 1.7, 1.5                  # 墩桩、台桩直径
PILE_LEN_PIER = 28.0                            # 【假设】桩长由地质定，此处取定值
PILE_LEN_ABUT = 20.0
TIE_W, TIE_H = 1.0, 1.2                         # 系梁
# 永久支座：平面尺寸按支座压应力验算选定（structure.py：Rck / Ae ≤ σc），取 50 mm 的整数倍；厚度【假设】。
BEARING_D, BEARING_T = 0.45, 0.077              # 伸缩端：圆形板式橡胶支座 φ450×77
# 连续墩（墩中心一排，承两跨）：矩形板式橡胶支座，顺桥向 500 × 横桥向 600 × 厚 99。顺桥向尺寸受现浇连续段
# 宽度限制（最窄 0.70 m，支座边缘还要离开预制梁端），所以用矩形而不是加大圆形直径。
BEARING_CONT_A, BEARING_CONT_B, BEARING_CONT_T = 0.50, 0.60, 0.099
SEAT_W, SEAT_W_CONT = 0.7, 0.8                  # 支座垫石平面尺寸（每边比支座宽出 0.1–0.15 m）
SEAT_MIN, SEAT_MAX = 0.15, 0.40                 # 垫石高度允许范围【假设】
TEMP_W, TEMP_MIN = 0.40, 0.10                   # 临时支座（砂筒）平面尺寸、最小高度【假设】
BARRIER_W, BARRIER_H = 0.5, 1.1                 # 混凝土护栏
LOFT_STEP = 1.5                                 # 铺装、护栏、翼缘现浇段沿路线的取样间距（跟随曲线）

# ------------------------------------------------------------------ 【几何】地形与被交道路
VALLEY_STATION = 10530.0
GROUND_BASE, VALLEY_DEPTH, VALLEY_WIDTH = 131.0, 12.0, 120.0
GROUND_SIDE_SLOPE = 0.015          # 横向坡度：向左抬高
ROAD_STATION = 10545.0             # 被交道路与路线正交处（P6–P7 跨中）
ROAD_WIDTH = 9.0
ROAD_CLEARANCE = 5.0               # 被交道路净高要求（设计输入）
ROAD_SIDE_CLEAR = 1.0              # 墩柱外缘到被交道路边线的最小距离【假设】

# ------------------------------------------------------------------ 【物理】
RC_DENSITY_T = 2.6                 # t/m³，钢筋混凝土（含钢筋）容重取 26 kN/m³

# ------------------------------------------------------------------ 【假设】钢筋含量（kg/m³ 混凝土）
REBAR_KG_M3 = {
    "girder": 190.0, "wet_joint": 180.0, "continuity": 220.0, "cap": 160.0, "abut_cap": 150.0,
    "backwall": 120.0, "column": 140.0, "tie": 120.0, "pile": 95.0, "seat": 100.0, "barrier": 110.0,
    "cantilever": 180.0, "diaphragm": 150.0,
}
CONCRETE_GRADE = {
    "girder": "C50", "wet_joint": "C50", "continuity": "C50", "cap": "C40", "abut_cap": "C40",
    "backwall": "C30", "column": "C40", "tie": "C30", "pile": "C30", "seat": "C50", "barrier": "C30",
    "cantilever": "C50", "pavement": "C40+沥青", "diaphragm": "C50",
}

# ------------------------------------------------------------------ 【规范】上部结构计算
# 下列取值逐条对过规范条文的在线转录版（jtg-d60.readthedocs.io、jtg-3362.readthedocs.io，非官方，个别处有错字）
# 或教材《桥梁工程》第 2 版（qlgc.readthedocs.io），没有对照印刷版；没对上的标【假设】。
E_C50 = 3.45e7                     # kN/m²，C50 弹性模量（JTG 3362-2018 表 3.1.5）
G_RATIO = 0.4                      # Gc = 0.4 Ec（JTG 3362-2018 第 3.1.6 条）
STIFF_FACTOR = 0.95                # 全预应力 / A 类构件挠度刚度 B0 = 0.95 Ec I0（JTG 3362-2018 式 6.5.2-4）
ETA_THETA = 1.45 - (50 - 40) / (80 - 40) * (1.45 - 1.35)   # C50 挠度长期增长系数（第 6.5.3 条，内插 = 1.425）
DEFLECTION_LIMIT = 1.0 / 600       # 汽车荷载（不计冲击）频遇组合长期挠度 ≤ L/600（第 6.5.3 条）
UNIT_RC, UNIT_ASPHALT, UNIT_LEVELLING = 26.0, 24.0, 25.0   # kN/m³（JTG D60-2015 表 4.2.1：25–26、23–24）
ASPHALT_T = 0.10                   # 铺装 0.20 m = 0.10 沥青混凝土 + 0.10 钢筋网混凝土调平层
Q_K = 10.5                         # kN/m，公路-I级车道均布荷载（JTG D60-2015 第 4.3.1 条）
PK_SHORT, PK_LONG = 270.0, 360.0   # kN，计算跨径 ≤ 5 m / ≥ 50 m 的集中荷载，中间直线内插（同上）
PK_SHEAR_FACTOR = 1.2              # 计算剪力效应时集中荷载乘 1.2（同上）
LANE_FACTORS = {1: 1.20, 2: 1.00, 3: 0.78, 4: 0.67}         # 横向车道布载系数（表 4.3.1-5）
WHEEL_GAUGE, WHEEL_TO_CURB, WHEEL_BETWEEN = 1.80, 0.50, 1.30  # 车辆横向布置（图 4.3.1-3，据教材转述）
GAMMA_0 = 1.1                      # 结构重要性系数：各等级公路上的大、中桥为一级（表 4.1.5-1）
GAMMA_G, GAMMA_G_FAV = 1.2, 1.0    # 混凝土结构重力分项系数，不利 / 有利（表 4.1.5-2）
GAMMA_Q1 = 1.4                     # 汽车荷载（车道荷载）分项系数（第 4.1.5 条）
PSI_F, PSI_Q = 0.7, 0.4            # 汽车荷载（不计冲击）频遇值、准永久值系数（第 4.1.6 条）
SIGMA_C = 10.0                     # MPa，板式橡胶支座使用阶段平均压应力限值【假设：JT/T 4 常用值，未核原文】
BEARING_COVER = 0.005              # 支座边缘橡胶保护层，有效承压直径 = d − 2×0.005【假设】
BEAM_ELEMENT = 0.75                # m，杆系模型单元长度上限
# 施工顺序【假设】：预制梁、横隔板、湿接缝与翼缘现浇段（一期恒载）由简支梁承担；
# 体系转换后，铺装与护栏（二期恒载，各片梁均分）和汽车荷载由连续梁承担。

# ------------------------------------------------------------------ 【假设】设备能力
ERECTOR_SWL_T = 120.0              # 架桥机额定起重
GANTRY_SWL_T = 50.0                # 梁场龙门吊单台额定起重
GANTRY_COUNT = 2                   # 两台抬吊
DUAL_LIFT_FACTOR = 0.8             # 抬吊折减
MIN_WET_JOINT = 0.45               # 相邻梁翼缘净距下限（湿接缝可施工）

# ------------------------------------------------------------------ 【假设】梁场与架梁计划
YARD_START = date(2026, 11, 16)    # 梁场首片梁开浇
ERECT_START = date(2026, 12, 7)    # 架桥机开始架梁（梁场提前 21 天）
N_BEDS = 16                        # 制梁台座数
BED_CYCLE_DAYS = 7                 # 单台座周期：绑扎、立模、浇筑、养护、张拉、移梁
MIN_AGE_DAYS = 14                  # 自开浇起至可架设（张拉、压浆强度）
MAX_STORAGE_DAYS = 90              # 存梁期上限（预应力梁上拱度随时间增长）
DAY_HOURS = 10.0                   # 架桥机每日有效工时，每周 7 天连续施工
DAY_START_HOUR = 8                 # 每日开工时刻（4D 任务时间用）
ERECT_HOURS = 2.0                  # 每片梁运梁、喂梁、落梁工时
LAUNCH_HOURS = 6.0                 # 每跨架完后过孔工时
TRANSFER_DAYS = 4                  # 左幅架完后架桥机转场到右幅起点
CONT_CAST_LAG_DAYS = 1             # 一联梁架完后第几天浇墩顶连续段
CONT_CURE_DAYS = 7                 # 连续段浇筑后到张拉负弯矩钢束、拆临时支座（体系转换）

# ------------------------------------------------------------------ 【假设】梁场布置（规划输入）
# 梁场设在 0 号桥台前的路基右侧，长轴平行于路线；布置图是输入，计划要装得进去才算通过。
YARD_STATION, YARD_OFFSET = 10150.0, -48.0   # 梁场局部原点（路线右侧 48 m）
STORAGE_POSITIONS, STORAGE_LAYERS = 20, 2    # 存梁台座 20 个、双层存梁：容量 40 片
BED_L, BED_W, BED_GAP = 32.0, 2.4, 2.0       # 制梁台座长、宽、间距
SLOT_W, SLOT_GAP = 2.2, 1.8                  # 存梁台座宽、间距（长同制梁台座）
REBAR_AREA = (24.0, 32.0)                    # 钢筋加工区
MIN_YARD_TO_ROAD = 25.0                      # 梁场边缘到路线中线的最小距离（不占路基）
